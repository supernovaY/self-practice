import React, { useState, useEffect, useRef, useContext } from 'react';
import { message, Modal } from 'antd';
import './index.less';

const personMove: React.FC = (): JSX.Element => {
  return (
    <div className="person-move-page">
      <h2>人员调动概况</h2>
    </div>
  );
};

export default personMove;
